name: CI/CD India Tourism
on:
 push:
  branch:
   -main

jobs:
 deploy:
  runs-on: ubutntu-latest

  steps:
   -name: checkout code
    uses: actions/checkout@v4
    
   -name: Test
    run: |
      python --version
      python -m py_compile app.py india.py
